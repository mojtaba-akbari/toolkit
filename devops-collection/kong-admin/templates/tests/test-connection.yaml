apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kong-admin.fullname" . }}-test-connection"
  labels:
    {{- include "kong-admin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kong-admin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
