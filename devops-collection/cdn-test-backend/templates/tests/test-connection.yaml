apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nginx-cdn.fullname" . }}-test-connection"
  labels:
    {{- include "nginx-cdn.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nginx-cdn.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
